package aufgabe7;

public interface Memory {
	
	//returns the available memory in Byte
	public float available();
	//reduces the memory with the given value - unit must be Byte
	public void use(float memory);
}
