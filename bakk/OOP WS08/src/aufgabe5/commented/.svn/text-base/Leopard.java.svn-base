package aufgabe5.commented;

/**
 * @author GEMEH, FLRAU, PAFAV
 * 
 * Leopard is a type of Racer
 * 
 */
public class Leopard extends Racer {
	int duration;
	
	public Leopard(int speed, int duration){
		// creates a racer with speed and duration
		super(speed);
		this.duration = duration;
	}
	
	public int getDuration(){
		// returns duration
		return duration;
	}

	public boolean faster(Racer that) {
		// expects a racer
		if(this.speed() > that.speed()) return true;
		return false;
		// true if this is faster than that
	}
	
}