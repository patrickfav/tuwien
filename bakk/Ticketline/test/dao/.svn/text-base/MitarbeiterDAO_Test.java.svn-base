package dao;


import static org.junit.Assert.assertSame;
import static org.junit.Assert.assertTrue;


import org.junit.After;
import org.junit.Before;
import org.junit.Test;

import ticketline.dao.DAOFactory;

import ticketline.dao.interfaces.MitarbeiterDAO;
import ticketline.dao.interfaces.OrtDAO;

import ticketline.db.Mitarbeiter;

/**
 * 
 * ArtikelDAO_Test 
 * @author AndiS
 * @version 0.1
 *
 */
public class MitarbeiterDAO_Test extends DAO_Test_Data
{

	private MitarbeiterDAO mitarbeiterDAO = DAOFactory.getMitarbeiterDAO();
	private OrtDAO ortDAO = DAOFactory.getOrtDAO();
	
	
	@Before 
	public void before()
	{

		//save
		//ortDAO.save(ort);
		//mitarbeiterDAO.save(mitarbeiter);
	}
	
	@After 
	public void after()
	{
		//delete
		//mitarbeiterDAO.remove(mitarbeiter);
		//ortDAO.remove(ort);
	}
	
	@Test 
	public void get() 
	{ 
		/*
		//search
		Mitarbeiter result = mitarbeiterDAO.get(mitarbeiter.getKartennr());

		//check
		assertSame(mitarbeiter.getTyp(),result.getTyp());
		assertSame(mitarbeiter.getOrtverk(),result.getOrtverk());
		assertSame(mitarbeiter.getBlz(),result.getBlz());
		assertSame(mitarbeiter.getKontonr(),result.getKontonr());
		assertSame(mitarbeiter.getPin(),result.getPin());
		assertSame(mitarbeiter.getBerechtigung(),result.getBerechtigung());
		assertSame(mitarbeiter.getPasswort(),result.getPasswort());
		assertSame(mitarbeiter.getTransaktionen(),result.getTransaktionen());
		assertSame(mitarbeiter.getUsername(),result.getUsername());
		*/
		//TODO
		assertTrue(true);
	}
		
	@Test 
	public void edit() 
	{ 
		/*
		//edit
		mitarbeiter.setBerechtigung("berechtigungEDIT");
		
		//save
		mitarbeiterDAO.save(mitarbeiter);
		
		//search
		Mitarbeiter result = mitarbeiterDAO.get(mitarbeiter.getKartennr());

		//check
		assertSame("kurzbezeichnungEDIT",result.getBerechtigung());
		*/
		//TODO
		assertTrue(true);
	}
}
